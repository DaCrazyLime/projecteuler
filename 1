n = int(input())         #Takes input and converts it to an integer
s=0                      #Set to 0 the sum of all the numbers that are divisible by either 3 or 5
for i in range (1,n,1):  #For 1 up to "n" (exclusive) with increment +1
    if i%3==0 or i%5==0: #Checks whether the current value of "i" is divisible by either 3 or 5
        s=s+i            #If true, is added to the variable "s".
print(s)                 #Gives ans, which should be 233168 if n = 1000 
